function Mx = monodromy_map(phases_0, period, parameters)
  global cds contopts
  int_opt = odeset(...
    'AbsTol',       contopts.monodromy_map_abs_tol, ...
    'RelTol',       contopts.monodromy_map_rel_tol, ...
    'Jacobian',     @(t,y) feval(cds.jacobian_ode, ...
                      t, deval(cds.cycle_orbit,t), parameters{:}) ...
  );                
  dydt_mon = @(t, y) ...
    cds.jacobian_ode(t, deval(cds.cycle_orbit,t), parameters{:}) * y;
  [~,orbit] = cds.integrator(dydt_mon, [0 period], phases_0, int_opt);
  Mx = orbit(end,:)';